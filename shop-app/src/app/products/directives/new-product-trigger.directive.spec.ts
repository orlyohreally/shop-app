import { NewProductTriggerDirective } from './new-product-trigger.directive';

describe('NewProductTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NewProductTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
